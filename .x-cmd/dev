# shellcheck shell=bash disable=SC1091,SC2034 #xsh

# Section: Arg
WORK_DIR="$(x wsroot)"
# EndSection

# Section: Main
if [ ! -d "$WORK_DIR/node_modules" ]; then
    x init none &&
    yarn dev
else
  yarn dev
fi
# EndSection
